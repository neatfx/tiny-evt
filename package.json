{
  "name": "tiny-evt",
  "description": "a tiny codebase for building electron-vue-typescript application with vite",
  "version": "0.2.0",
  "author": "Robocat<neatfx@gmail.com>",
  "main": "build/main.js",
  "scripts": {
    "dev-prebuild": "esbuild --platform=node --bundle --external:vite --external:esbuild scripts/dev-runner.ts --outfile=build/dev-runner.js",
    "dev": "npm run dev-prebuild && env-cmd -r configs/.env-cmdrc.json -e development node build/dev-runner.js",
    "dist-prebuild": "esbuild --platform=node --bundle --external:vite --external:esbuild --external:electron-builder scripts/app-builder.ts --outfile=build/app-builder.js",
    "dist": "npm run dist-prebuild && node build/app-builder.js",
    "cypress": "npm run dev-prebuild && env-cmd -r configs/.env-cmdrc.json -e development,test node build/dev-runner.js"
  },
  "dependencies": {
    "vue-router": "^4.0.0-alpha.12"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.0.0-beta.14",
    "cypress": "^4.6.0",
    "electron": "^9.0.0",
    "electron-builder": "^22.6.0",
    "env-cmd": "^10.1.0",
    "vite": "^0.17.0"
  }
}
